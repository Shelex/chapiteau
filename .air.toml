# .air.toml
[build]
  # Binary file path
  bin = "bin/chapiteau"
  # Watch these directories for changes
  include_ext = ["go", "tpl", "tmpl", "html"]
  exclude_dir = ["assets", "tmp", "vendor", "web"]
  stop_on_error = true
  cmd = "make build"

[log]
  level = "debug"